At:r.startedAt||Date.now()}}catch(n){throw n.name==="TypeError"||n.code==="ECONNREFUSED"||n.code==="ETIMEDOUT"?new tye("Network error during token refresh",n):n}}async getAuthRequest(e){let r=new URL(kR.AUTH,this.config.apiBaseUrl);r.searchParams.set("client_type","extension"),r.searchParams.set("callback_url",e),r.searchParams.set("redirect_uri",e);let n;try{if(n=await Vi(r.toString(),{method:"GET",redirect:"manual",credentials:"include",headers:await this.headers()}),n.status>=300&&n.status<400){let s=n.headers.get("Location");if(!s)throw new Error("No redirect URL found in the response");re
